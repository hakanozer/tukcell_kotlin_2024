<!-- res/drawable/border_with_radius.xml -->

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Arka plan rengi -->
    <stroke
        android:width="3dp"
        android:color="@color/black" /> <!-- Border rengi -->
    <corners
        android:radius="16dp" /> <!-- Border köşe yarıçapı -->
</shape>
